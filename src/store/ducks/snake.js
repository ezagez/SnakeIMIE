// Actions

// Reducer
const initial = {

}

export default function reducer(state = initial, action = {}) {
    switch (action.type) {
        default:
            return state
    }
}

// Action Creator